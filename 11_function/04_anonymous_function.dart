// Anonymous Function

// What
// - A function which doesn't have name.

// Example
void main() {
  // Storing anonymous function in variable.
  var variable = (int a, int b) {
    return a + b;
  };
  print(variable(1, 2));
}
