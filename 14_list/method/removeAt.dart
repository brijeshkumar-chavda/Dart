// .removeAt()

// What
// - It will remove the element at specified index in the list.

// Note
// - It will modify the original list.

// Syntax
// list.removeAt(index);

// Example
void main() {
  var list = [1, 2, 3, 4, 5];

  list.removeAt(0);
  print(list);
}