// .insert()

// What
// - It will insert the element at specified index in the list.

// Note
// - It will modify the original list.

// Syntax
// list.insert(index, element);

// Example
void main() {
  var list = [1, 2, 3, 4, 5];

  list.insert(0, 10);
  print(list);
}
