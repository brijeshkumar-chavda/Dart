void main() {
  // NOTE
  // Dynamic type is used when you don't know the type of the variable before you use it.

  // Can be initialized later
  dynamic variable;
  variable = "value";

  // Can be reassign it
  variable = 99.99;
  print(variable);

  // Can't be redeclared
}
