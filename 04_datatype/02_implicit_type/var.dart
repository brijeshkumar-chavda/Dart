void main() {
  var variableName = "value";

  // var is not dynamic
  // variableName = 123;

  // you can reassign it but value should be of same type
  variableName = "new value";
  print(variableName);
}
